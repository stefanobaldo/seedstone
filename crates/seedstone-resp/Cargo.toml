[package]
name = "seedstone-resp"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "SeedStone RESP2 codec."
publish = false

[dependencies]
# The bulk's bytes travel on as a `Bytes` so the layers above can share
# them without copying; it is the codec's only dependency.
bytes.workspace = true

[lints]
workspace = true
