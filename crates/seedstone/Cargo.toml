[package]
name = "seedstone"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "An in-memory key-value store, written from scratch in Rust. Bootstrap phase: no functionality yet."
publish = false

[dependencies]
seedstone-core = { path = "../seedstone-core" }

[lints]
workspace = true
