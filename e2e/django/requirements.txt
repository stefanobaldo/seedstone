# The client pair this gate exercises, frozen deliberately. See README.md.
django-redis==4.12.1
redis==4.1.4
Django==3.0.14
pytest==7.4.4
# Not a client, and not optional. The suite's test module imports the msgpack
# serializer at import time, so without this nothing collects at all.
msgpack==1.0.5
# Also not optional. The client's lock retries acquisition forever, so a lock
# this server cannot grant is an infinite wait rather than a failure — and a
# lane that hangs cannot be red, green, or read. A per-test bound turns every
# such wait into an ordinary failure the expectations file can carry.
pytest-timeout==2.1.0
