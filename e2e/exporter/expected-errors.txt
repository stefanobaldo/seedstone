# Error lines the exporter is expected to log today, matched as substrings.
# One per line. Each names a command this server does not answer yet; the
# line leaves when the command arrives. An empty file is this lane's gate.
